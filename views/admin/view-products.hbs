<section>
  <div class="container">
    <div class="row mt-3 d-flex">
      <a href="/admin/add-products" class="btn btn-success btn-sm me-auto ">Add Product</a>
    </div>
    <table class="table mt-3">
      <thead>
        <tr>
          <th scope="col">no</th>
          <th scope="col">Title</th>
          <th scope="col">Category</th>
          <th scope="col">Description</th>
          <th scope="col">Price</th>
          <th scope="col">Image</th>
          <th scope="col">Edit</th>
          <th scope="col">Delete</th>
        </tr>
      </thead>
      <tbody>
        {{#each products}}
          <tr>
            <td>{{inc @index}}</td>
            <td>{{this.Name}}</td>
            <td>{{this.Category}}</td>
            <td>{{this.Description}}</td>
            <td>₹{{this.Price}}</td>
            <td><img
                src="/product-images/{{this._id}}.jpg"

                alt="{{this.Name}}"
                style="width:50px ; height :50px"
              /></td>
               <td><a href="/admin/edit-product/{{this._id}}" class="btn btn-primary"> Edit</a></td>
              <td><a href="/admin/delete-product/{{this._id}}" class="btn btn-danger" onclick="return confirm('Are you sure you want to delete {{this.Name}} ?')"> Delete</a></td>
             
          </tr>
        {{/each}}
      </tbody>
    </table>
  </div>
</section>